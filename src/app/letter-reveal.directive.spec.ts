import { LetterRevealDirective } from './letter-reveal.directive';

describe('LetterRevealDirective', () => {
  it('should create an instance', () => {
    const directive = new LetterRevealDirective();
    expect(directive).toBeTruthy();
  });
});
